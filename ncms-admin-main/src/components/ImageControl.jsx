// Image upload/preview control — re-exports the NCET design-system control
// (preview + Upload/Change/Remove via the global upload modal).
export { ImageControl as default, ImageControl } from "./shared";
