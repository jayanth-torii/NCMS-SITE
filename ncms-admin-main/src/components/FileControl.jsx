// File (PDF/doc) control — re-exports the NCET design-system control
// (View + Upload/Change + Edit link + Remove via the global upload modal).
export { FileControl as default, FileControl } from "./shared";
